/**Performance Benchmarks*/ export class BenchmarkRunner { private results: Map<string, number[]> = new Map(); async runBenchmark(name: string, fn: () => Promise<void>, iterations: number = 100): Promise<number> { const times: number[] = []; for (let i = 0; i < iterations; i++) { const start = performance.now(); await fn(); const end = performance.now(); times.push(end - start); } this.results.set(name, times); return times.reduce((a, b) => a + b) / times.length; } getResults(name: string): { avg: number; min: number; max: number } { const times = this.results.get(name) || []; return { avg: times.reduce((a, b) => a + b, 0) / times.length, min: Math.min(...times), max: Math.max(...times) }; } }
