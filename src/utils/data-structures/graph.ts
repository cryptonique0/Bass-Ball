/**Graph Data Structure*/ export class Graph { private vertices: Set<string> = new Set(); private edges: Map<string, Set<string>> = new Map(); addVertex(vertex: string): void { this.vertices.add(vertex); if (!this.edges.has(vertex)) { this.edges.set(vertex, new Set()); } } addEdge(v1: string, v2: string): void { this.addVertex(v1); this.addVertex(v2); this.edges.get(v1)?.add(v2); } getNeighbors(vertex: string): string[] { return Array.from(this.edges.get(vertex) || new Set()); } }
