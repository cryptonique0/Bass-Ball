/**Mobile/Responsive UI*/ export class TouchControlManager { private touches: Map<number, { x: number; y: number }> = new Map(); handleTouchStart(touches: Array<{ identifier: number; clientX: number; clientY: number }>): void { for (const touch of touches) { this.touches.set(touch.identifier, { x: touch.clientX, y: touch.clientY }); } } handleTouchMove(touches: Array<{ identifier: number; clientX: number; clientY: number }>): void { for (const touch of touches) { this.touches.set(touch.identifier, { x: touch.clientX, y: touch.clientY }); } } handleTouchEnd(touchIds: number[]): void { for (const id of touchIds) { this.touches.delete(id); } } getTouchPosition(touchId: number): { x: number; y: number } | undefined { return this.touches.get(touchId); } }
