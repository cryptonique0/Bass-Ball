/**Admin Dashboard*/ export class AdminDashboard { private banList: Set<string> = new Set(); private auditLogs: Array<{ timestamp: number; action: string; admin: string; target: string }> = []; banPlayer(playerId: string, adminId: string): void { this.banList.add(playerId); this.logAction(adminId, 'BAN_PLAYER', playerId); } unbanPlayer(playerId: string, adminId: string): void { this.banList.delete(playerId); this.logAction(adminId, 'UNBAN_PLAYER', playerId); } isPlayerBanned(playerId: string): boolean { return this.banList.has(playerId); } private logAction(admin: string, action: string, target: string): void { this.auditLogs.push({ timestamp: Date.now(), action, admin, target }); } getAuditLog(): Array<any> { return [...this.auditLogs]; } }
