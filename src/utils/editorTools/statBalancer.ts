/**Player Stat Balancer*/ export class PlayerStatBalancer { balanceStats(stats: Record<string, number>): Record<string, number> { const avg = Object.values(stats).reduce((a, b) => a + b) / Object.keys(stats).length; return Object.fromEntries(Object.entries(stats).map(([k, v]) => [k, v * (75 / avg)])); } validateBalance(stats: Record<string, number>): boolean { return Object.values(stats).every((v) => v >= 50 && v <= 99); } }
