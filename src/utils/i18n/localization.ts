/**Localization System*/ export class LocalizationManager { private translations: Map<string, Map<string, string>> = new Map(); addTranslation(language: string, key: string, value: string): void { if (!this.translations.has(language)) { this.translations.set(language, new Map()); } this.translations.get(language)!.set(key, value); } translate(language: string, key: string): string { return this.translations.get(language)?.get(key) || key; } getSupportedLanguages(): string[] { return Array.from(this.translations.keys()); } }
