/**Custom Mode Builder*/ export class CustomModeBuilder { private modes: Array<{ id: string; rules: Record<string, unknown>; name: string }> = []; createCustomMode(name: string, rules: Record<string, unknown>): string { const id = `mode_${Date.now()}`; this.modes.push({ id, name, rules }); return id; } updateMode(id: string, rules: Record<string, unknown>): void { const mode = this.modes.find((m) => m.id === id); if (mode) { mode.rules = rules; } } deleteMode(id: string): void { this.modes = this.modes.filter((m) => m.id !== id); } getCustomModes(): Array<any> { return this.modes; } }
