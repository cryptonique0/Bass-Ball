/**Puzzle Mode*/ export class PuzzleMode { private puzzles: Array<{ id: string; difficulty: number; solved: boolean }> = []; createPuzzle(difficulty: number): string { const id = `puzzle_${Date.now()}`; this.puzzles.push({ id, difficulty, solved: false }); return id; } solvePuzzle(puzzleId: string): boolean { const puzzle = this.puzzles.find((p) => p.id === puzzleId); if (puzzle) { puzzle.solved = true; return true; } return false; } getSolvedCount(): number { return this.puzzles.filter((p) => p.solved).length; } }
