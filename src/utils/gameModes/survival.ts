/**Survival Mode*/ export class SurvivalMode { private wave: number = 1; private difficulty: number = 1; private enemyCount: number = 10; advanceWave(): void { this.wave++; this.difficulty *= 1.15; this.enemyCount = Math.floor(10 + this.wave * 5); } getCurrentWave(): number { return this.wave; } getEnemyCount(): number { return this.enemyCount; } getDifficulty(): number { return this.difficulty; } }
