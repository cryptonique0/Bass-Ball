/**Battle Royale Mode*/ export class BattleRoyaleMode { private players: Set<string> = new Set(); private eliminations: Map<string, string> = new Map(); addPlayer(playerId: string): void { this.players.add(playerId); } eliminatePlayer(playerId: string, eliminatedBy: string): void { this.players.delete(playerId); this.eliminations.set(playerId, eliminatedBy); } getRemainingPlayers(): number { return this.players.size; } getWinner(): string | null { return this.players.size === 1 ? Array.from(this.players)[0] : null; } }
