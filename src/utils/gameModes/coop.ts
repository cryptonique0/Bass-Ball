/**Co-op Game Mode*/ export class CoopGameMode { private players: string[] = []; private objectives: Array<{ id: string; completed: boolean }> = []; startCoopMatch(playerIds: string[]): void { this.players = playerIds; } completeObjective(objectiveId: string): void { const obj = this.objectives.find((o) => o.id === objectiveId); if (obj) obj.completed = true; } getProgress(): number { return this.objectives.filter((o) => o.completed).length / this.objectives.length; } }
