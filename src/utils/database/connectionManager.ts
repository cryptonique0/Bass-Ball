/**Database Connection Manager*/ export class DatabaseConnectionManager { private connections: Map<string, any> = new Map(); async connect(dbName: string, url: string): Promise<void> { this.connections.set(dbName, { connected: true, url }); } disconnect(dbName: string): void { this.connections.delete(dbName); } isConnected(dbName: string): boolean { return this.connections.has(dbName); } }
